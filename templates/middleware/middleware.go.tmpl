package middleware

import "go.uber.org/zap"

{{if not .NoRequestID -}}
// CtxLoggerKey is the http.Request Context lookup key for the request ID logger
const CtxLoggerKey = "request_id_logger"
{{- end}}

// Middleware exposes useful variables to every middleware handler.
// If you wanted to pass additional objects to each abcweb middleware handler
// you can add them here, and then assign them in app/middleware.go where the
// instance of middleware is created.
type Middleware struct {
	{{if .NoRequestID -}}
	// Log is used for logging in your middleware.
	// Do not remove.
	{{- else -}}
	// Log is used for logging in your middleware and to 
	// create a derived logger that includes the request ID.
	// Do not remove.
	{{- end}}
	Log *zap.Logger

	// Your custom vars go here.
}
